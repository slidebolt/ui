# PostgreSQL
DB_USER=
DB_PASSWORD=
DB_NAME=slidebolt

# API
PORT=40001
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@localhost:5432/${DB_NAME}
SESSION_SECRET=
INTERNAL_SERVICE_URL=http://127.0.0.1:39011
NODE_ENV=production

# Default system user passwords (set on first startup, ignored if users exist)
DEFAULT_ADMIN_PASSWORD=
DEFAULT_OPERATOR_PASSWORD=
DEFAULT_VIEWER_PASSWORD=
